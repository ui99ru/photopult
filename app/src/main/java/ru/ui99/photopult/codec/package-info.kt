/**
 * Low-latency H.264 preview codec: hardware MediaCodec encoder on the camera, decoder on the
 * remote, plus the adaptive bitrate/resolution ladder. Populated in Stages 3–4.
 */
package ru.ui99.photopult.codec
